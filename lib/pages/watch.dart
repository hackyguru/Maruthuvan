import 'package:flutter/material.dart';

class Watch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Watchie Watchie")),
      backgroundColor: Colors.greenAccent,
    );
  }
}
